/**
 * Game Configuration Constants
 */
export const CONFIG = {
    // Maze settings
    MAZE_SIZE: 9,
    CELL_SIZE: 5,
    WALL_HEIGHT: 5,

    // Player settings
    PLAYER_RADIUS: 0.4,
    PLAYER_SPEED: 8,
    TURN_SPEED: 3,
    CATCH_DISTANCE: 1.0,

    // Game timing
    GAMEPLAY_DURATION: 10,
    MAP_DURATION: 5,

    // Visual settings
    FOG_DENSITY: 0.055,

    // Camera bob
    BOB_SPEED: 12,
    BOB_AMOUNT: 0.08
};
